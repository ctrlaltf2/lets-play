# Protobuf styleguide

This is probably the shortest style guide ever:

- `edition = "2023"`, use it.
	- To expand on this, Protobuf editions are going to be how the Protocol Buffers language
		is extended in future versions; so we should just commit to them.

That's it.
